{
  "name": "react-sandbox",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "php server.php",
    "build": "browserify -t [ reactify --harmony ] public/scripts/tutorial2.js -o public/scripts/bundle.js"
  },
  "dependencies": {
    "body-parser": "^1.4.3",
    "express": "^4.4.5"
  },
  "browserify": {
    "transform": [
      [ "reactify", { "harmony": true } ]
    ]
  },
  "devDependencies": {
    "jquery": "^2.1.3",
    "marked": "^0.3.3",
    "react": "^0.13.0",
    "reactify": "^1.0.0"
  }
}
